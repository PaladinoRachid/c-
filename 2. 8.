#include <iostream>
#include <iomanip>

using namespace std;

float med(float km, float l)
{ 
    float med;
    med = km/l;
    return med;
}    

int main()
{
    float a, b;
    cin >> a >> b;
    cout << fixed << setprecision (3);
    cout << med(a,b) << " km/l";

    return 0;
}
