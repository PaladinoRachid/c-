#include <iostream>

using namespace std;

void divisores (int a)
{
    int b;
    b = 1;
    while (a >= b)
    {
        if (a%b == 0)
        {
         cout << b << " ";
        }
        b++;
    }
    return;
}

int main()
{
    int a;
    cin >> a;
    divisores(a);
}
