#include <iostream>

using namespace std;

void seq1()
{
    int a, b, c;
    cin >> a >> b;
    
    if (a >= b)
    {
        for (c = a; c >= b; c-- )
        {
            cout << c << " ";
        }
        

    }
    else
    {
       for (c = a; c <= b; c++ )
        {
            cout << c << " ";
        }
    }    
        return;
}
int main()
{
    seq1();
    return 0;
}
