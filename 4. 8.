#include <iostream>

using namespace std;

void imprime ( int n1, int n2, int x)
{
    int c;
if ( n1 < n2)
{
    c = n1;
    while ( c <= n2)
    {
        if ( c%x == 5)
        {
        cout << c << " ";
        }
        c++;
    }
}
else 
{
    c = n2;
    while ( c <= n1)
    {
        if ( c%x == 5)
        {
        cout << c << " ";
        }
        c++;
    }
} return;
}

int main()
{
    int n1, n2, x;
    cin>> n1 >> n2 >> x;
    imprime(n1,n2,x);
    return 0;
}
